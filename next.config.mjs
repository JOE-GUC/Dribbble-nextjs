/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'pixabay.com',
            "lh3.googleusercontent.com",
            'codewithgeeks.onrender.com'
        ]
    },
};

export default nextConfig;
